# Notes Frontend

React SPA for managing notes, styled with the Ocean Professional theme.

Scripts:
- npm start
- npm run build
- npm test

Environment:
- REACT_APP_API_BASE_URL (defaults to http://localhost:3001)

Run:
1) Copy .env.example to .env if you want to override API base URL.
2) npm install
3) npm start
